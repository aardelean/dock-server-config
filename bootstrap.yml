spring:
  cloud:
    consul:
      host: 192.168.178.19
      port: 8500
ribbon:
  eureka:
    enabled:  false
zuul:
  ignoreLocalService: false
  routes:
    backendClient:
      path: /api/home
      serviceId:  backend-client

