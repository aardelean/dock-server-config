spring:
  cloud:
    consul:
      host: 192.168.178.19
      port: 8500
zuul:
  ignoreLocalService: false
  routes:
    backendClient:
      path: /api/home
      serviceId:  backend-server
backend-server:
  ribbon:
    listOfServers:  localhost:9023
