config:
  name: Remote zwei
zuul:
  ignoreLocalService: false
  routes:
    backendClient:
      path: /api/gist
      serviceId:  backend-server
backend-server:
  ribbon:
    listOfServers:  localhost:9023
